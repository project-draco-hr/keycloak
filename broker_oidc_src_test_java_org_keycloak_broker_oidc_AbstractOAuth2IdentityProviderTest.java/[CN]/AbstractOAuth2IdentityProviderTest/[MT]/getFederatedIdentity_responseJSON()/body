{
  TestProvider tested=getTested();
  Map<String,String> notes=new HashMap<>();
  FederatedIdentity fi=tested.getFederatedIdentity(notes,"{\"" + AbstractOAuth2IdentityProvider.OAUTH2_PARAMETER_ACCESS_TOKEN + "\" : \"458rt\"}");
  Assert.assertNotNull(fi);
  Assert.assertEquals("458rt",fi.getId());
}
