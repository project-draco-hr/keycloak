{
  TestProvider tested=getTested();
  Map<String,String> notes=new HashMap<>();
  tested.getFederatedIdentity(notes,"{\"" + AbstractOAuth2IdentityProvider.OAUTH2_PARAMETER_ACCESS_TOKEN + "\" : \"\"}");
}
