{
  try {
    if (Desktop.isDesktopSupported()) {
      loginDesktop();
    }
 else {
      loginManual();
    }
  }
 catch (  Throwable e) {
    System.err.println("Failed to log in user: " + e.getMessage());
  }
}
