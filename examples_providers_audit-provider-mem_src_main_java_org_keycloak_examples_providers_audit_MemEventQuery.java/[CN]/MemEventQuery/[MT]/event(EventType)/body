{
  Iterator<Event> itr=this.events.iterator();
  while (itr.hasNext()) {
    Event next=itr.next();
    for (    EventType e : events) {
      if (next.getEvent().equals(e)) {
        break;
      }
    }
    itr.remove();
  }
  return this;
}
