{
  boolean validated=validateAnswer(context);
  if (!validated) {
    Response challenge=loginForm(context).setError("badSecret").createForm("secret_question.ftl");
    context.failureChallenge(AuthenticationFlowError.INVALID_CREDENTIALS,challenge);
    return;
  }
  setCookie(context);
  context.success();
}
