{
  formData=new HashMap<String,String>();
  @SuppressWarnings("unchecked") MultivaluedMap<String,String> t=(MultivaluedMap<String,String>)request.getAttribute("KEYCLOAK_FORM_DATA");
  if (t != null) {
    for (    String k : t.keySet()) {
      formData.put(k,t.getFirst(k));
    }
  }
}
