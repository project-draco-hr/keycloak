{
  String subsystemXml="<subsystem xmlns=\"" + KeycloakExtension.NAMESPACE + "\">"+ "</subsystem>";
  KernelServices services=super.installInController(subsystemXml);
  super.assertRemoveSubsystemResources(services);
}
