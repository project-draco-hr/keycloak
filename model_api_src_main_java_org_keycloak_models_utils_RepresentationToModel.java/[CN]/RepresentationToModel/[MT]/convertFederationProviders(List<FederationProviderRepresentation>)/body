{
  List<FederationProviderModel> result=new ArrayList<FederationProviderModel>();
  for (  FederationProviderRepresentation representation : providers) {
    FederationProviderModel model=new FederationProviderModel(representation.getId(),representation.getProviderName(),representation.getConfig());
    result.add(model);
  }
  return result;
}
