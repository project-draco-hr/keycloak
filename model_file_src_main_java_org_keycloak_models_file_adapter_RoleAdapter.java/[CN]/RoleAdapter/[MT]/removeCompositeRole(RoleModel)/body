{
  compositeRoles.remove(childRole);
  List<String> compositeRoleIds=role.getCompositeRoleIds();
  if (compositeRoleIds == null)   return;
  compositeRoleIds.remove(childRole.getId());
  role.setCompositeRoleIds(compositeRoleIds);
}
