{
  JpaModel client=(JpaModel)adapters.get(id);
  if (client != null) {
    if (em.contains(client.getEntity())) {
      em.flush();
      em.refresh(client.getEntity());
      return client;
    }
  }
  return null;
}
