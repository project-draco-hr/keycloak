{
  RealmEntity realmEntity=em.getReference(RealmEntity.class,realm.getId());
  Object count=em.createNamedQuery("getRealmUserCount").setParameter("realm",realmEntity).getSingleResult();
  return ((Number)count).intValue();
}
