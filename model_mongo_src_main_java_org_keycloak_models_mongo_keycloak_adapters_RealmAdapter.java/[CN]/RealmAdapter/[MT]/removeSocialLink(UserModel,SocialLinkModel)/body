{
  UserEntity userEntity=((UserAdapter)user).getUser();
  String userId=userEntity.getId();
  DBObject query=new QueryBuilder().and("socialProvider").is(socialLink.getSocialProvider()).and("socialUsername").is(socialLink.getSocialUsername()).and("userId").is(userId).get();
  mongoStore.removeObjects(SocialLinkEntity.class,query);
}
