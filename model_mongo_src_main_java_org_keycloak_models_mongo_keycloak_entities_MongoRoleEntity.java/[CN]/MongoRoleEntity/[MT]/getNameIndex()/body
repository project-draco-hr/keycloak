{
  String realmId=getRealmId();
  String applicationId=getApplicationId();
  String name=getName();
  if (realmId != null) {
    return realmId + "//" + name;
  }
 else {
    return applicationId + "//" + name;
  }
}
