{
  String realmId=getRealmId();
  String clientId=getClientId();
  String name=getName();
  if (realmId != null) {
    return realmId + "//" + name;
  }
 else {
    return clientId + "//" + name;
  }
}
