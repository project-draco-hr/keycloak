{
  if (lastSync == null) {
    String val=getConfig().getFirst("lastSync");
    if (val == null) {
      lastSync=0;
    }
 else {
      lastSync=Integer.valueOf(val);
    }
  }
  return lastSync;
}
