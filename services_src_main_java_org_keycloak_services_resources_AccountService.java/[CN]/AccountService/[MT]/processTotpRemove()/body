{
  UserModel user=getUserFromAuthManager();
  user.setTotp(false);
  return Flows.forms(realm,request,uriInfo).setError("successTotpRemoved").setErrorType(FormFlows.ErrorType.SUCCESS).setUser(user).forwardToTotp();
}
