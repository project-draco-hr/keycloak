{
  URI redirect=AdminRoot.adminConsoleUrl(uriInfo).path("index.html").build(realm.getName());
  return Response.status(302).location(OpenIDConnectService.logoutUrl(uriInfo).queryParam("redirect_uri",redirect.toString()).build(realm.getName())).build();
}
