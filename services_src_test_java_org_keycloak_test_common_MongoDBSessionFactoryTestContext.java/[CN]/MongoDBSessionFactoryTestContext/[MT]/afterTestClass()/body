{
  if (mongodExe != null) {
    if (mongod != null) {
      mongod.stop();
    }
    mongodExe.stop();
  }
  logger.info("MongoDB stopped successfully");
  System.setProperty(KeycloakApplication.SESSION_FACTORY,"");
}
