{
  if (container == null) {
    return false;
  }
  return container.getName().matches("a.*-server-wildfly") || container.getName().matches("a.*-server-eap.") || container.getName().equals("app-server-as7");
}
