{
  if (response.getAccessToken() != null) {
    if (DefaultBruteForceProtector.exceptionObject != null) {
      DefaultBruteForceProtector.exceptionObject.printStackTrace();
    }
  }
  Assert.assertNull("was mark set: " + DefaultBruteForceProtector.markNotBefore + " time test: "+ DefaultBruteForceProtector.markCheck+ " test count "+ DefaultBruteForceProtector.testCount+ " nullFailure: "+ DefaultBruteForceProtector.nullFailureCount+ " logFailureCalled: "+ DefaultBruteForceProtector.logFailureCalled+ " logFailure: "+ DefaultBruteForceProtector.logFailure+ " notFoundUserId: "+ DefaultBruteForceProtector.notFoundUserId+ " exception: "+ DefaultBruteForceProtector.exception,response.getAccessToken());
}
