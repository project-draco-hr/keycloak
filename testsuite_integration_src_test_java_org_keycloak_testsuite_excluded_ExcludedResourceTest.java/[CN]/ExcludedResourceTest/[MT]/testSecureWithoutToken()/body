{
  Response post=ClientBuilder.newClient().target("http://localhost:8081/app").path("secure").request().post(Entity.entity("Hallo",MediaType.TEXT_PLAIN_TYPE));
  Assert.assertEquals(302,post.getStatusInfo().getStatusCode());
}
